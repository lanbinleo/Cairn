# Cairn 文案与术语规范

界面文案的唯一规范来源。写任何用户可见的 UI 文字（按钮、标题、描述、空态、报错、tooltip、浮窗脚本、报错 toast）之前先读这一页；AI 辅助开发时把这一页喂给模型。规则来自 0.3.1 产品经理文案审查（背景：产品在跟用户讲自己的实现，界面像「仪表盘后面贴满工程图纸」）。

## 总原则

1. **界面是汽车仪表盘，不是工程图纸。** 用户关心「这笔交易怎么样」，不关心数据结构。不解释 UI 自己（“核心状态与组成，一屏可见”这类给评审看的句子直接删）。
2. **计算口径说明挂 ⓘ（`components/info-hint.tsx`），不常驻占位。** 用户第一次需要它，第一百次不需要。R 口径、计划 vs 实际来源、过程分哲学、AI 原则宣言都是这么处理的。正面示范：交易表 PnL% 的 tooltip。
3. **原则/价值观类文案只出现一次。** “只摆事实，不打分——过程评价永远是你自己的”这类话放 tooltip 或首次引导，不要每次渲染。
4. **同一事实只在一处说全。** 侧栏是扫一眼的大数字，细节活在对应 tab；状态 badge 和只读文本不重复出现。
5. **空态要么给下一步动作，要么不渲染。** 不为“没有东西”画一张大卡。

## 术语表（用户可见文字统一用这些）

| 概念 | 用词 | 禁用 |
| --- | --- | --- |
| 卡片 AI 分析（一次动作） | AI 识别 / 重新识别 / 全部识别 / 识别中… | AI 整理、整理中、待重新识别（作为入口动词） |
| 分析结果过期 | 需重新识别 | 过期、已过期、待重新识别（三说法已统一） |
| 从未跑过分析 | 未识别 | 未整理 |
| 编辑 AI 提取的标签 | 编辑标签 | 整理标签 |
| 持仓管理建议 | AI 补录建议 | AI 检查 |
| 交易标签建议（0.3.4） | AI 标签建议 | AI 打标签、自动标签、智能标签 |
| 重拆已有卡片（0.3.4） | AI 重拆此卡 / 重拆中… | 重新拆卡、再拆一次、AI 拆分 |
| 交易评分筛选（0.3.4） | 已评分 / 未评分（未评分时侧栏入口「未评分，去评分」） | 评分完成、scored |
| 数据写进本地库 | 记录 / 已记录 | 落库（工程师黑话） |
| 卡片原文 | 原文 / 卡片 | 原始 Card |
| 排列方式说明 | （不解释） | “按创建顺序排列”、“按展示阶段” |
| Trade 详情三个 tab | 复盘 / 案例 / 评估 | Overview / Case / Trade（递归命名） |
| 时间线 | 时间线 | Timeline |
| 图表叠加线 | 轨迹线 / 入场线 | Trail line / Entry line |
| K 线数量 | N 根K线 | N bars |
| 记录方式（下拉） | 当时记录 / 事后补录 | Forward · 当时记录 / Retrospective · 事后记录 |
| 防事后偏差 | 防事后诸葛 | 防 hindsight |
| 计划外动作 | 计划外动作 | unplanned 动作 |
| AI 任务状态（任务中心） | 进行中 / 已完成；图标 = spinner（进行中）/ 圆圈勾（成功）/ 叹号（失败） | 加载中、处理中等模糊词 |
| 网络类报错 | 分类人话（连接超时 / 无法建立连接·检查 VPN / 模型服务返回 4xx/5xx + 提示） | 裸英文错误串、只给 URL 不给原因 |
| 出站代理模式（0.3.3） | 跟随系统代理 / 手动指定 / 直连 | 「系统代理模式」「proxy mode」等英文混排 |
| 失败任务确认（任务中心） | 知道了，不再提示 | 忽略错误、清除失败 |

## 中英规则

- **领域词保留英文**：BAR、R、PnL、Case、Card、Trade、Period、Execution、Profit Factor、EMA、UTC、JSON、memo（Entry 卡语境）。这些是交易者词汇，翻译反而增加认知负担。
- **UI 结构词必须中文**：时间线、轨迹线、入场线、根（K 线）、复盘/案例/评估 tab、元信息。
- 一个词要么一直中文要么一直英文，同一屏不混用两种风格。

## 保留的品牌调性

- 「心路历程」是刻意的品牌语言，保留；同语气的新功能命名保持这种温度（卡片阶段提示语「这张卡可以覆盖：…」同属此类）。
- AI 功能的动词保持「秘书感」：AI 识别、AI 找 Case / AI 找 Trade、AI 拟题、AI 总结、AI 补录建议——都是「替用户干活」，不是「展示智能」。

## 确认与提示（0.3.5）

- 确认弹窗（`useConfirm()`，全局唯一）：标题 = 一句话问句（「删除 Case「X」？」），正文只补后果（「其全部 Cards 会一起删除（软删除，可从备份恢复）。」）；主按钮用动词原形（删除 / 清空 / 重新生成 / 重新识别 / 解除关联 / 开始重拆），破坏性动作用红键；取消恒为「取消」。Esc / 点外部一律等于取消。
- toast（sonner，右上角）：成功 = 「已 + 动词」短句（已删除 Case / 已填入复盘备注），路径等细节放 description；失败 / 警告沿用「一句人话 + 下一步」；技术原因进日志，不进 toast。
- 字段级校验、导入向导分步状态、AI 任务中心仍走就地展示，不弹 toast。

## 数值与格式

- 金额单行放不下用 `fmtCompactMoney`（≥1 万 K/M/B），完整值放 title 悬停；不要让统计卡换行。
- 报错文案 = 一句人话 + 下一步。技术原因（网络/解析/Provider 配置）进日志（Rust `log_provider_event` / 前端日志），不糊用户脸上。

## 变更纪律

- 改任何用户可见文案时同步检查本页术语表；新增术语先在这里登记再用。
- 浮窗脚本 `scripts/cairn-case-widget.user.js` 的文案同受本页约束（改脚本记得 bump `@version`）。
